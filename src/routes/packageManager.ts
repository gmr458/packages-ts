import { Router } from 'express';
// import { ctrlPackageManager } from '../controllers/index';

const router: Router = Router();

// router.route('/add').post(ctrlPackageManager.addPkgManager);

// router.route('/add-multiple').post(ctrlPackageManager.addMultiplePkgManagers);

// router.route('/one/:id').get(ctrlPackageManager.onePkgManager);

// router.route('/some').get(ctrlPackageManager.somePkgsManager);

// router.route('/list').get(ctrlPackageManager.listPkgManagers);

// router.route('/update/:id').put(ctrlPackageManager.updatePkgManager);

// router.route('/update').put(ctrlPackageManager.updateMultiplePkgManager);

// router.route('/delete/:id').delete(ctrlPackageManager.deletePkgManager);

// router.route('/delete').delete(ctrlPackageManager.deleteMultiplePkgManager);

export default router;
